<tr>
	<td>
		<h3>
			<%= link_to product.title, product_path(product) %> 
		</h3>
	</td>
	<td>
			<%= product.description %><br>
	</td>
	<td>
			<%= product.price %><br>
	</td>
	<td>
			<%= product.category.title %><br>
	</td>
	<td>
			<%= product.difficulty_level %><br>
	</td>
	<td>
			<%= product.country_of_origin %><br>
	</td>
	<td>
			<%= link_to "edit", edit_product_path(product) %>
			<%= link_to "delete", product_path(product), method: :delete, data: {confirm: "Are you sure to delete this product?"} %>
	</td>
</tr>